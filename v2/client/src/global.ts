interface Global {
    backgroundColor: string;
    primaryColor: string;
    secondaryColor: string;
    textColor: string;
    accentColor: string;
    dangerColor: string;
}

const global: Global = {
    backgroundColor: '#062F04',
    primaryColor: '#86E981',
    secondaryColor: '#366933',
    textColor: '#FFFFFF',
    accentColor: '#ffc445',
    dangerColor: '#b33'
}

const getCSSConstantsFromFile = () => {
    //From ./styles/global.scss
    

}

export default global;