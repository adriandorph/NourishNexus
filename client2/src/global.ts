
interface Global {
    backgroundColor: string;
    primaryColor: string;
    secondaryColor: string;
    textColor: string;
    accentColor: string;
}

const global: Global = {
    backgroundColor: '#062F04;',
    primaryColor: '#86E981',
    secondaryColor: '#366933',
    textColor: '#FFFFFF',
    accentColor: '#DBAC34'
}

export default global;